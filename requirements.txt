# Only the LGPL-compatible Qt Core/Gui/Widgets runtime used by the app.
# Do not replace with the PySide6 meta-package: it installs Addons and causes
# unrelated Qt modules/plugins to enter frozen bundles.
PySide6-Essentials==6.10.3
numpy>=1.24
Pillow>=10.0
tifffile>=2023.7          # OME-TIFF output for stitched mosaics
imagecodecs>=2024.1       # lossless tiled BigTIFF input/output
scipy>=1.11               # sparse robust pose solve for wide-area mosaics
