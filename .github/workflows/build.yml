name: Build standalone apps

# Build macOS (.app) and Windows (folder) bundles.
# - On a version tag (v1.0.0, ...) the zips are attached to a GitHub Release.
# - Manual runs (workflow_dispatch) upload them as workflow artifacts you can download.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write   # needed to create/attach releases

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # macos-latest runners are Apple Silicon; a build made there will not run
          # on Intel Macs, so build both. macos-13 was retired, so Intel builds use
          # macos-15-intel.
          - os: macos-latest
            artifact: BZ-Plate-Studio-macOS-appleSilicon
            bundle: "dist/BZ Plate Studio.app"
          - os: macos-15-intel
            artifact: BZ-Plate-Studio-macOS-intel
            bundle: "dist/BZ Plate Studio.app"
          - os: windows-latest
            artifact: BZ-Plate-Studio-Windows
            bundle: "dist/BZ Plate Studio"
    runs-on: ${{ matrix.os }}
    # A retired runner label queues forever instead of failing; cap it so a bad
    # label is visible in minutes rather than blocking the release.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Build with PyInstaller
        run: pyinstaller --noconfirm ktf_viewer.spec

      - name: Zip bundle (macOS)
        if: runner.os == 'macOS'
        run: |
          cd dist
          zip -ry "../${{ matrix.artifact }}.zip" "BZ Plate Studio.app"

      - name: Zip bundle (Windows)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path "dist/BZ Plate Studio/*" -DestinationPath "${{ matrix.artifact }}.zip"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.zip

      - name: Attach to Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}.zip
